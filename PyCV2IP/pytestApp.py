import numpy as np
import cv2IP

# a = np.arange(24)
# b = np.arange(24)
# c = np.arange(24)
# C = np.vstack((a,b))
# D = print(np.hstack((A,B)))
# E = np.vstack((C,c))
# print(E)
# print(len(E))

red = cv2IP.ColorType(1)
print(red)